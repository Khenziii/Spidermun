I think that there is no need for sql databases such as sqlite.
The project is so small that i think .dat and .json files will be sufficient.
(however if you want, you can always implement them and create a PR :>).